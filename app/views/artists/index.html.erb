<% unless flash[:alert].blank? %>
  <h1><%= flash[:alert] %></h1>
<% end %>
<h1>Artists</h1>
<ul>
  <% @artists.each do |artist| %>
    <li><%= link_to artist.name, artist_path(artist) %> - <%= link_to artist.name, artist_songs_path(artist) %></li>
  <% end %>
</ul>
